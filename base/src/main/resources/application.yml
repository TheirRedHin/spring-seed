spring:
  application:
    name: base
  web:
    resources:
      add-mappings: false
  profiles:
    active: dev
  # rabbitmq 配置
  rabbitmq:
    host: 192.168.1.36
    port: 5672
    username: admin
    password: admin
  # 数据库配置
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://192.168.1.36:3308/aa?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
          username: root
          password: root
          driver-class-name: com.mysql.cj.jdbc.Driver
# 服务端口
server:
  port: 8888